import React from "react";
import { createRoot } from "react-dom/client";
import "./styles.css";
function App(){return <main><p className="eyebrow">Design Studio / Integration slot</p><h1>AI Campaign Production</h1><p className="lede">The Studio contract is ready. Replace this placeholder with the existing V0 application when GitHub becomes the shared source of truth.</p><section><span>Stable slug</span><code>/ai-campaign-production</code></section><section><span>Agent context</span><code>../../AGENTS.md + prototype.json + ../../context/</code></section></main>}
createRoot(document.getElementById("root")).render(<App/>);
