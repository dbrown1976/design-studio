# Visual language

The Studio does not impose one visual style on every prototype. Product fidelity
should come from the system being explored.

Shared defaults for the Studio shell itself:

- quiet neutral chrome;
- typography and spacing that keep the prototype, not the Studio, dominant;
- status expressed with text as well as colour;
- no decorative UI that implies workflow state which does not exist.
