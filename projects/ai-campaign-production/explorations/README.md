# Exploration sets

No exploration sets have been created for AI Campaign Production yet.

When the first divergent exploration starts, add a JSON document here that
conforms to `schemas/exploration-set.schema.json`. It must identify the accepted
revision it branches from with `basedOnRevision`.
